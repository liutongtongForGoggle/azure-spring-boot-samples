apiVersion: v1
kind: Service
metadata:
    name: "k8s-c771"
    labels:
        app: "k8s-c771"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "k8s-c771"